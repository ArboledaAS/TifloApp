<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arboleda.tifloapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TifloApp">
        <activity android:name=".menulibros.EditBook"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".AuthActivity" />
        <activity android:name=".menus.AuthActivity" />
        <activity android:name=".menus.MasterMenu" />
        <activity android:name=".menulibros.CreateBook" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>